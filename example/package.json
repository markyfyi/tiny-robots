{
  "name": "tiny-robots-app",
  "version": "0.0.4",
  "license": "UNLICENSED",
  "scripts": {
    "dev": "tiny-robots",
    "export": "tiny-robots export --view-source",
    "export-dev": "rm -fr export && tiny-robots export --dev --view-source"
  },
  "dependencies": {
    "svelte": "^3.37.0",
    "tiny-robots": "^0.0.35"
  }
}

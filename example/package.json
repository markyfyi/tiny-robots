{
  "name": "tiny-robots-app",
  "version": "0.0.4",
  "license": "UNLICENSED",
  "scripts": {
    "dev": "tiny-robots",
    "export": "tiny-robots export",
    "export-dev": "rm -fr export && tiny-robots export --dev"
  },
  "dependencies": {
    "tiny-robots": "^0.0.4"
  }
}
